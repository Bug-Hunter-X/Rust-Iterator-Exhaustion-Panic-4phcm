fn main() {    let mut vec = Vec::new();    vec.push(1);    vec.push(2);    vec.push(3);    let mut iter = vec.iter();    if let Some(value) = iter.next() {        println!("{:?}", value);    }    if let Some(value) = iter.next() {        println!("{:?}", value);    }    if let Some(value) = iter.next() {        println!("{:?}", value);    }    // This will not panic because we check for None    if let Some(value) = iter.next() {        println!("{:?}", value);    } else {        println!("Iterator exhausted");    }   } 